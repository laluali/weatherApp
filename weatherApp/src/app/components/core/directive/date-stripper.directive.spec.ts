import { DateStripperDirective } from './date-stripper.directive';

describe('DateStripperDirective', () => {
  it('should create an instance', () => {
    const directive = new DateStripperDirective();
    expect(directive).toBeTruthy();
  });
});
